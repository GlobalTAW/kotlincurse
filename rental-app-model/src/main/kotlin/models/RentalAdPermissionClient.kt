package ru.teterin.rentalapp.model.models

enum class RentalAdPermissionClient {
    READ,
    UPDATE,
    DELETE,
    BOOK,
    MAKE_VISIBLE_PUBLIC,
    MAKE_VISIBLE_GROUP,
    MAKE_VISIBLE_OWNER,
}
